name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install yt-dlp pyinstaller
    
    - name: Build executable
      run: |
        pyinstaller --onefile --noconsole --add-data "assets;assets" --icon "assets/icon.ico" --name "Iker2YT" src/downloader_windows.py
    
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/Iker2YT.exe
        name: "Iker2YT ${{ github.ref_name }}"
        body: |
          🎉 **Nuevo ejecutable de Iker2YT para Windows**
          
          ✅ Compatible con Windows 11/10 64-bit  
          ✅ No requiere instalación de Python  
          ✅ Solo descargar y ejecutar  
          
          **Instrucciones:**
          1. Descargar `Iker2YT.exe`
          2. Doble clic para ejecutar
          3. Pegar URL de YouTube
          4. ¡Disfrutar!
    
    - name: Upload artifact (if not release)
      if: "!startsWith(github.ref, 'refs/tags/')"
      uses: actions/upload-artifact@v4
      with:
        name: Iker2YT-Windows
        path: dist/Iker2YT.exe
